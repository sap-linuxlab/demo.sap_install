credential:
  scm:
    name: << SCM Name >>
    user: << SCM Username >>
    pwd:  << SCM Password >>
  ee:
    name: << Registry Name >>
    user: << Registry Username >>
    pwd: << Registry Password >>
    host: << Registry Auth URL >>
  machine:
    name: << Machine Credential Name >>
    login: << Machine Crendential Login name >>
